# CompanyExpenses

Static expense tracking dashboard for recording company expenses, budgets, categories, statuses, links, charts, JSON backup/restore, and Excel export.

## Run locally

Open `outputs/index.html` in a browser.

## Firebase Hosting

This project is configured to deploy the `outputs/` folder to Firebase Hosting project `companyexpenses-3d15b`.

```bash
firebase deploy
```

